spring:
  application:
    name: book-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/gallerydb

server:
  port: 8081

eureka:
  client:
    service-url:
      default_zone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true