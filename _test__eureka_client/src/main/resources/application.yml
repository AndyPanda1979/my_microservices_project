server:
  port: 8881

eureka:
  client:
    register-with-eureka: true
    enabled: true
    service-url:
      default_zone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true

spring:
  application:
    name: test_eureka_client